server:
  http_listen_port: 9080

clients:
  - url: http://loki:3100/loki/api/v1/push

positions:
  filename: /tmp/positions.yaml

scrape_configs:
  - job_name: "docker-logs"
    static_configs:
      - targets: [ "192.168.122.100" ]
        labels:
          job: docker-logs
          __path__: /var/log/*log
